{
  "name": "quillscript",
  "version": "1.0.0",
  "description": "Fully passive narrative improvements for AI Dungeon",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:min": "node build.js --minify",
    "build:pretty": "node build.js --format=pretty",
    "build:minified": "node build.js --minify --format=minified"
  },
  "keywords": [
    "quillscript",
    "aidungeon",
    "ai",
    "adventure"
  ],
  "author": "UmbraQuill",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.8.1",
    "terser": "^5.46.0"
  }
}
